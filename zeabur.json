{
  "$schema": "https://schema.zeabur.app/zeabur.json",
  "build": {
    "python": {
      "version": "3.11"
    }
  },
  "start": {
    "command": "cd scripts && gunicorn quick_capture_webhook:app --bind 0.0.0.0:$PORT"
  }
}
